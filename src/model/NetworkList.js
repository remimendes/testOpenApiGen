/**
 * test opensvc agent api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.12.19
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Network from './Network';

/**
 * The NetworkList model module.
 * @module model/NetworkList
 * @version 3.12.19
 */
class NetworkList {
    /**
     * Constructs a new <code>NetworkList</code>.
     * @alias module:model/NetworkList
     * @param kind {module:model/NetworkList.KindEnum} 
     * @param items {Array.<module:model/Network>} 
     */
    constructor(kind, items) { 
        
        NetworkList.initialize(this, kind, items);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind, items) { 
        obj['kind'] = kind;
        obj['items'] = items;
    }

    /**
     * Constructs a <code>NetworkList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NetworkList} obj Optional instance to populate.
     * @return {module:model/NetworkList} The populated <code>NetworkList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NetworkList();

            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [Network]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NetworkList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NetworkList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NetworkList.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                Network.validateJSON(item);
            };
        }

        return true;
    }


}

NetworkList.RequiredProperties = ["kind", "items"];

/**
 * @member {module:model/NetworkList.KindEnum} kind
 */
NetworkList.prototype['kind'] = undefined;

/**
 * @member {Array.<module:model/Network>} items
 */
NetworkList.prototype['items'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
NetworkList['KindEnum'] = {

    /**
     * value: "NetworkList"
     * @const
     */
    "NetworkList": "NetworkList"
};



export default NetworkList;

