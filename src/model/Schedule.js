/**
 * test opensvc agent api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.12.19
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Schedule model module.
 * @module model/Schedule
 * @version 3.12.19
 */
class Schedule {
    /**
     * Constructs a new <code>Schedule</code>.
     * @alias module:model/Schedule
     * @param action {String} 
     * @param schedule {String} 
     * @param key {String} 
     * @param lastRunAt {Date} 
     * @param lastRunFile {String} 
     * @param lastSuccessFile {String} 
     * @param nextRunAt {Date} 
     * @param requireCollector {Boolean} 
     * @param requireProvisioned {Boolean} 
     */
    constructor(action, schedule, key, lastRunAt, lastRunFile, lastSuccessFile, nextRunAt, requireCollector, requireProvisioned) { 
        
        Schedule.initialize(this, action, schedule, key, lastRunAt, lastRunFile, lastSuccessFile, nextRunAt, requireCollector, requireProvisioned);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, action, schedule, key, lastRunAt, lastRunFile, lastSuccessFile, nextRunAt, requireCollector, requireProvisioned) { 
        obj['action'] = action;
        obj['schedule'] = schedule;
        obj['key'] = key;
        obj['last_run_at'] = lastRunAt;
        obj['last_run_file'] = lastRunFile;
        obj['last_success_file'] = lastSuccessFile;
        obj['next_run_at'] = nextRunAt;
        obj['require_collector'] = requireCollector;
        obj['require_provisioned'] = requireProvisioned;
    }

    /**
     * Constructs a <code>Schedule</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Schedule} obj Optional instance to populate.
     * @return {module:model/Schedule} The populated <code>Schedule</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Schedule();

            if (data.hasOwnProperty('action')) {
                obj['action'] = ApiClient.convertToType(data['action'], 'String');
            }
            if (data.hasOwnProperty('schedule')) {
                obj['schedule'] = ApiClient.convertToType(data['schedule'], 'String');
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('last_run_at')) {
                obj['last_run_at'] = ApiClient.convertToType(data['last_run_at'], 'Date');
            }
            if (data.hasOwnProperty('last_run_file')) {
                obj['last_run_file'] = ApiClient.convertToType(data['last_run_file'], 'String');
            }
            if (data.hasOwnProperty('last_success_file')) {
                obj['last_success_file'] = ApiClient.convertToType(data['last_success_file'], 'String');
            }
            if (data.hasOwnProperty('next_run_at')) {
                obj['next_run_at'] = ApiClient.convertToType(data['next_run_at'], 'Date');
            }
            if (data.hasOwnProperty('require_collector')) {
                obj['require_collector'] = ApiClient.convertToType(data['require_collector'], 'Boolean');
            }
            if (data.hasOwnProperty('require_provisioned')) {
                obj['require_provisioned'] = ApiClient.convertToType(data['require_provisioned'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Schedule</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Schedule</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Schedule.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['action'] && !(typeof data['action'] === 'string' || data['action'] instanceof String)) {
            throw new Error("Expected the field `action` to be a primitive type in the JSON string but got " + data['action']);
        }
        // ensure the json data is a string
        if (data['schedule'] && !(typeof data['schedule'] === 'string' || data['schedule'] instanceof String)) {
            throw new Error("Expected the field `schedule` to be a primitive type in the JSON string but got " + data['schedule']);
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['last_run_file'] && !(typeof data['last_run_file'] === 'string' || data['last_run_file'] instanceof String)) {
            throw new Error("Expected the field `last_run_file` to be a primitive type in the JSON string but got " + data['last_run_file']);
        }
        // ensure the json data is a string
        if (data['last_success_file'] && !(typeof data['last_success_file'] === 'string' || data['last_success_file'] instanceof String)) {
            throw new Error("Expected the field `last_success_file` to be a primitive type in the JSON string but got " + data['last_success_file']);
        }

        return true;
    }


}

Schedule.RequiredProperties = ["action", "schedule", "key", "last_run_at", "last_run_file", "last_success_file", "next_run_at", "require_collector", "require_provisioned"];

/**
 * @member {String} action
 */
Schedule.prototype['action'] = undefined;

/**
 * @member {String} schedule
 */
Schedule.prototype['schedule'] = undefined;

/**
 * @member {String} key
 */
Schedule.prototype['key'] = undefined;

/**
 * @member {Date} last_run_at
 */
Schedule.prototype['last_run_at'] = undefined;

/**
 * @member {String} last_run_file
 */
Schedule.prototype['last_run_file'] = undefined;

/**
 * @member {String} last_success_file
 */
Schedule.prototype['last_success_file'] = undefined;

/**
 * @member {Date} next_run_at
 */
Schedule.prototype['next_run_at'] = undefined;

/**
 * @member {Boolean} require_collector
 */
Schedule.prototype['require_collector'] = undefined;

/**
 * @member {Boolean} require_provisioned
 */
Schedule.prototype['require_provisioned'] = undefined;






export default Schedule;

