/**
 * test opensvc agent api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.12.19
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KeywordMeta model module.
 * @module model/KeywordMeta
 * @version 3.12.19
 */
class KeywordMeta {
    /**
     * Constructs a new <code>KeywordMeta</code>.
     * @alias module:model/KeywordMeta
     * @param object {String} 
     * @param node {String} 
     * @param keyword {String} 
     * @param isEvaluated {Boolean} 
     * @param evaluatedAs {String} 
     */
    constructor(object, node, keyword, isEvaluated, evaluatedAs) { 
        
        KeywordMeta.initialize(this, object, node, keyword, isEvaluated, evaluatedAs);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, object, node, keyword, isEvaluated, evaluatedAs) { 
        obj['object'] = object;
        obj['node'] = node;
        obj['keyword'] = keyword;
        obj['is_evaluated'] = isEvaluated;
        obj['evaluated_as'] = evaluatedAs;
    }

    /**
     * Constructs a <code>KeywordMeta</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KeywordMeta} obj Optional instance to populate.
     * @return {module:model/KeywordMeta} The populated <code>KeywordMeta</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KeywordMeta();

            if (data.hasOwnProperty('object')) {
                obj['object'] = ApiClient.convertToType(data['object'], 'String');
            }
            if (data.hasOwnProperty('node')) {
                obj['node'] = ApiClient.convertToType(data['node'], 'String');
            }
            if (data.hasOwnProperty('keyword')) {
                obj['keyword'] = ApiClient.convertToType(data['keyword'], 'String');
            }
            if (data.hasOwnProperty('is_evaluated')) {
                obj['is_evaluated'] = ApiClient.convertToType(data['is_evaluated'], 'Boolean');
            }
            if (data.hasOwnProperty('evaluated_as')) {
                obj['evaluated_as'] = ApiClient.convertToType(data['evaluated_as'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KeywordMeta</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KeywordMeta</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KeywordMeta.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['object'] && !(typeof data['object'] === 'string' || data['object'] instanceof String)) {
            throw new Error("Expected the field `object` to be a primitive type in the JSON string but got " + data['object']);
        }
        // ensure the json data is a string
        if (data['node'] && !(typeof data['node'] === 'string' || data['node'] instanceof String)) {
            throw new Error("Expected the field `node` to be a primitive type in the JSON string but got " + data['node']);
        }
        // ensure the json data is a string
        if (data['keyword'] && !(typeof data['keyword'] === 'string' || data['keyword'] instanceof String)) {
            throw new Error("Expected the field `keyword` to be a primitive type in the JSON string but got " + data['keyword']);
        }
        // ensure the json data is a string
        if (data['evaluated_as'] && !(typeof data['evaluated_as'] === 'string' || data['evaluated_as'] instanceof String)) {
            throw new Error("Expected the field `evaluated_as` to be a primitive type in the JSON string but got " + data['evaluated_as']);
        }

        return true;
    }


}

KeywordMeta.RequiredProperties = ["object", "node", "keyword", "is_evaluated", "evaluated_as"];

/**
 * @member {String} object
 */
KeywordMeta.prototype['object'] = undefined;

/**
 * @member {String} node
 */
KeywordMeta.prototype['node'] = undefined;

/**
 * @member {String} keyword
 */
KeywordMeta.prototype['keyword'] = undefined;

/**
 * @member {Boolean} is_evaluated
 */
KeywordMeta.prototype['is_evaluated'] = undefined;

/**
 * @member {String} evaluated_as
 */
KeywordMeta.prototype['evaluated_as'] = undefined;






export default KeywordMeta;

