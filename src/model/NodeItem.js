/**
 * test opensvc agent api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 3.12.19
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Node from './Node';
import NodeMeta from './NodeMeta';

/**
 * The NodeItem model module.
 * @module model/NodeItem
 * @version 3.12.19
 */
class NodeItem {
    /**
     * Constructs a new <code>NodeItem</code>.
     * @alias module:model/NodeItem
     * @param kind {module:model/NodeItem.KindEnum} 
     * @param meta {module:model/NodeMeta} 
     * @param data {module:model/Node} 
     */
    constructor(kind, meta, data) { 
        
        NodeItem.initialize(this, kind, meta, data);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, kind, meta, data) { 
        obj['kind'] = kind;
        obj['meta'] = meta;
        obj['data'] = data;
    }

    /**
     * Constructs a <code>NodeItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NodeItem} obj Optional instance to populate.
     * @return {module:model/NodeItem} The populated <code>NodeItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NodeItem();

            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = NodeMeta.constructFromObject(data['meta']);
            }
            if (data.hasOwnProperty('data')) {
                obj['data'] = Node.constructFromObject(data['data']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NodeItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NodeItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NodeItem.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          NodeMeta.validateJSON(data['meta']);
        }
        // validate the optional field `data`
        if (data['data']) { // data not null
          Node.validateJSON(data['data']);
        }

        return true;
    }


}

NodeItem.RequiredProperties = ["kind", "meta", "data"];

/**
 * @member {module:model/NodeItem.KindEnum} kind
 */
NodeItem.prototype['kind'] = undefined;

/**
 * @member {module:model/NodeMeta} meta
 */
NodeItem.prototype['meta'] = undefined;

/**
 * @member {module:model/Node} data
 */
NodeItem.prototype['data'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
NodeItem['KindEnum'] = {

    /**
     * value: "NodeItem"
     * @const
     */
    "NodeItem": "NodeItem"
};



export default NodeItem;

